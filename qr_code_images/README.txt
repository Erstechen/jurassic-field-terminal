Printable QR code images for the physical scavenger hunt.

These are scanned by the in-app camera — they are NOT used as dinosaur artwork in the UI.

Each image should encode the JSON from the matching file in /qr/ (e.g. brachiosaurus.json).
